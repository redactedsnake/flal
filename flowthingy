<!DOCTYPE html>
<html>
<head>
  <title>Flowlab Alert</title>
  <script>
    window.onload = function () {
      const params = new URLSearchParams(window.location.search);
      const msg = params.get('msg');
      if (msg) {
        alert(msg);
      }
    };
  </script>
</head>
<body>
  <h1>Flowlab Alert Page</h1>
  <p>To show an alert, add <code>?msg=YourMessage</code> to the URL.</p>
</body>
</html>
